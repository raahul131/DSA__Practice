#include<iostream>
#include<vector>
using namespace std;

int main() {

    vector<vector<int>> twoDVector; // initialoization of the 2D vector

    int n, m;
    cin >> n >> m;
    vector<vector<int>> vec(n,vector<int>(m)); // initailozation of the 2D vector with specifyying the size where n is row size and m is the col size;

    

    return 0;
}